export const metadata = {
  title: 'Recuperar Senha - Zent',
  description: 'Recupere seu acesso redefinindo sua senha',
};

export default function ForgotPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
