export const metadata = {
  title: 'Redefinir Senha - Zent',
  description: 'Redefinir sua senha com o link de recuperação',
};

export default function ResetPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
